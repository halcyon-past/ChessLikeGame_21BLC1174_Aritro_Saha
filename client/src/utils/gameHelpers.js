// Example utility function for calculating valid moves
export const calculateValidMoves = (board, position) => {
    // Implement your logic to calculate valid moves based on the board state and position
    return [];
  };
  
  // Example utility function for checking move validity
  export const isValidMove = (board, from, to) => {
    // Implement your logic to check if a move is valid
    return true;
  };
  